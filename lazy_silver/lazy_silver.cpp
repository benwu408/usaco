#include <iostream>
#include <vector>
#include <string>
#include <map>
using namespace std;

int main() {
    int n, k;
    cin >> n >> k;
    vector<vector<int> > field(n);

    for (int y = 0; y < n; y ++){
        for (int x = 0; x < n; x ++){
            int grass;
            cin >> grass;
            field[y].push_back(grass);
        }
    }
    
    for (int y = 0; y < n; y ++){
        for (int x = 0; x < n; x ++){
            int sum = 0;
            for (int x )
        }
    }

}